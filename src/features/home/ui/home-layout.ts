/** ホーム画面のユーザーバーを下方向へずらす量（px） */
export const HOME_TOP_LAYOUT_OFFSET = 28;

/** ホーム背景画像を下方向へずらす量（px・ユーザーバーとは独立） */
export const HOME_BACKGROUND_OFFSET = 48;

/** ガチャ玉・対人対戦アイコンの共通 top（画面基準・ユーザーバーとは独立） */
export const HOME_SIDE_ACTION_TOP = 192;

/** ガチャ玉アイコンサイズ（px） */
export const HOME_GACHA_ICON_SIZE = 84;

/** ホーム画面「設定」ボタン（ユーザーバー下・ガチャ玉アイコン上） */
export const HOME_SETTINGS_BUTTON_TOP = 142;
export const HOME_SETTINGS_BUTTON_LEFT = 14;
export const HOME_SETTINGS_BUTTON_WIDTH = 96;
export const HOME_SETTINGS_BUTTON_HEIGHT = 64;

/** 対人対戦バッジの高さ（px） */
export const HOME_PVP_BADGE_HEIGHT = 112;

/** 対人対戦バッジの幅（px） */
export const HOME_PVP_BADGE_WIDTH = 240;

/** 対人対戦バッジの right（px・負の値で画面右端からはみ出し） */
export const HOME_PVP_BUTTON_RIGHT = -32;

/** 「タイトルへ」ボタンの top（画面右上寄せ） */
export const HOME_TITLE_BACK_BUTTON_TOP = 124;

/** 「タイトルへ」ボタン画像のサイズ（px） */
export const HOME_TITLE_BACK_BUTTON_WIDTH = 170;
export const HOME_TITLE_BACK_BUTTON_HEIGHT = 80;

/** 「タイトルへ」ボタンの right（px・負の値で右端にはみ出し） */
export const HOME_TITLE_BACK_BUTTON_RIGHT = -12;

/** ホーム下部メニュー「駒ガチャ」画像の高さ（px） */
export const HOME_PIECE_GACHA_IMAGE_HEIGHT = 58;

/** ガチャ玉ビューア「ヘルプ」ボタン画像のサイズ（px） */
export const HOME_GACHA_BALL_HELP_BUTTON_WIDTH = 220;
export const HOME_GACHA_BALL_HELP_BUTTON_HEIGHT = 120;

/** ガチャ玉ビューア「ヘルプ」ボタンの right（px・負の値で右端寄せ） */
export const HOME_GACHA_BALL_HELP_BUTTON_RIGHT = -30;
